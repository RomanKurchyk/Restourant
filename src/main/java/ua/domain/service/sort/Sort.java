package ua.domain.service.sort;

import ua.domain.api.Identifier;

import java.util.Comparator;
import java.util.List;

public class Sort implements Sortable {
    @Override
    public List<Identifier> sort(List<Identifier> items, Comparator comparator) {
        return null;
    }
}
